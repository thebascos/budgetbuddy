import { BillFilterPipe } from './bill-filter.pipe';

describe('BillFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BillFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
