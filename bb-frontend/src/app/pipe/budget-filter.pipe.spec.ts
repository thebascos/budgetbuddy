import { BudgetFilterPipe } from './budget-filter.pipe';

describe('BudgetFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BudgetFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
